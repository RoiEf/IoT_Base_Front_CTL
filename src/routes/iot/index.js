import { h } from 'preact';
import style from './style';

// import {ApiAddress} from '../../components/api';

const Iot = () => {

	return(<div class={style.iot}>
		<h1>IoT Specific Settings</h1>
		<p>This is the >IoT Specific Settings component.</p>
	</div>);
};

export default Iot;
