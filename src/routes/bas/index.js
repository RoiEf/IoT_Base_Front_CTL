import { h } from 'preact';
import style from './style';

// import {ApiAddress} from '../../components/api';

const Bas = () => {

	return(<div class={style.basic}>
		<h1>Basic Settings</h1>
		<p>This is the Basic Settings component.</p>
	</div>);
};

export default Bas;
