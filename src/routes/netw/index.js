import { h } from 'preact';
import style from './style';

// import {ApiAddress} from '../../components/api';

const Netw = () => {

	return(<div class={style.network}>
		<h1>Network Settings</h1>
		<p>This is the Network Settings component.</p>
	</div>);
};

export default Netw;
