import { h } from 'preact';
import style from './style';

// import {ApiAddress} from '../../components/api';

const Updates = () => {

	return(<div class={style.updates}>
		<h1>Updates</h1>
		<p>This is the Updates component.</p>
	</div>);
};

export default Updates;
